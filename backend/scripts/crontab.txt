# 数据库备份定时任务

# 每天凌晨2点执行备份
0 2 * * * cd /path/to/project/backend && node scripts/backup.js >> logs/backup.log 2>&1

# 或使用 shell 脚本
# 0 2 * * * cd /path/to/project/backend && bash scripts/backup.sh >> logs/backup.log 2>&1
